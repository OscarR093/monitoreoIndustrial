## 1. Backend

- [x] 1.1 Mover `alarmService.VerificarAsync` dentro de `GuardarDatoSensorAsync`, condicionado a `guardarDato == true`
- [ ] 1.2 Eliminar el bloque `if (esRealtime && sensor != null)` del loop principal

## 2. Verification

- [ ] 2.1 `dotnet build` sin errores
